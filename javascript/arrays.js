const arreglos =['texto', 456 , true , {propedad: 'valor'} , [1, 2, 3]]

// arreglos.map((valor)=>
// console.log(valor)
// )
const persona = {
nombre:'carlos',
edad: 27,
carro: {
    marca:'...',
    color: 'negro'

}
}
// console.log(persona.carro.marca);

const arreglo =['texto', 4433, false, {propiedad:'valor'},[1,2,3]]

// arreglo.map((valor, index)=>
// console.log(valor,index)
// )
// arreglo.forEach ((element) => {
//     console.log(element)
// });
const colores =[];
colores[0]= "morado"
colores.push = "azul"
console.log(colores);
